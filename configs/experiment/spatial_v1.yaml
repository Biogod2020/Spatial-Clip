# configs/experiment/spatial_v1.yaml
# @package _global_

# CodeGuardian: 实验配置文件的唯一职责是“覆盖”默认值。
# 它不包含自己的 `defaults` 列表。
# 结构清晰，一目了然地展示了本次实验与默认配置的所有不同之处。

# --- 实验元数据 ---
tags: ["Spatial-CLIP", "ViT-B-32", "Spatial-Loss", "Baseline"]
name: "spatial_v1_baseline_run_1" # Aim/WandB 中的运行名称

# --- 针对本次实验的参数覆盖 ---
# 直接指定要覆盖的组件及其参数

trainer:
  devices: 1
  max_epochs: 100

data:
  batch_size: 1500
  num_workers: 16

optimizer:
  lr: 1e-4

scheduler:
  num_warmup_steps: 1000

# 如果需要覆盖 logger 的特定参数，也可以在这里进行
# logger:
#   aim:
#     experiment: "Spatial-CLIP-High-Batch-Size"